let itemNames = [`dogs`,`cats`,`birds`];
let itemPrices = [30, 20, 0]
let inStock = [true, false, true,];

let firstItemName = itemNames[0];
console.log(firstItemName);

let firstItemPrice = itemPrices[0];
console.log(firstItemPrice);

itemNames.push(`snakes`);
itemPrices.push(100);
inStock.push(false);

lastItemName = itemNames.pop(3)
lastItemPrice = itemPrices.pop(3)
lastItemStock = inStock.pop(3)








 